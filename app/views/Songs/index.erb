<% @songs.each do |song| %>
  <div class="song">
    <h3><a href="/songs/<%= song.id %>"><%= song.title %></a> <small>(by: <%= song.author%>)</small></h3>
    <p>
      <%= song.url %>
      <small>created at: <%= song.created_at %></small>
    </p>
  </div>

<% end %>
