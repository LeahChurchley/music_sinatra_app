<% @songs.each do |song| %>
  <div class="song">
    <h3><%= song.title %> <small>(by: <%= song.author%>)</small></h3>
    <p>
      <%= song.url %>
      <small>created at: <%= song.created_at %></small>
    </p>
  </div>



<% end %>
